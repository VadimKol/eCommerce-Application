export enum NavigationPaths {
  HOME = '/',
  CATALOG = '/catalog',
  ABOUT = '/about',
}

export enum ActionPaths {
  LOGIN = '/login',
  REGISTER = '/register',
  PROFILE = '/profile',
  CART = '/cart',
}

export enum CurrencySymbols {
  USD = '$',
  EUR = '€',
}
